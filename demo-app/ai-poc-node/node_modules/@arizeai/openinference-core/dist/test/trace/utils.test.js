"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const src_1 = require("../../src");
const api_1 = require("@opentelemetry/api");
describe("withSafety", () => {
    afterEach(() => {
        jest.clearAllMocks();
        jest.resetModules();
        jest.restoreAllMocks();
    });
    it("should return a function", () => {
        const safeFunction = (0, src_1.withSafety)({ fn: () => { } });
        expect(typeof safeFunction).toBe("function");
    });
    it("should execute the provided function without errors", () => {
        const mockFn = jest.fn();
        const safeFunction = (0, src_1.withSafety)({ fn: mockFn });
        safeFunction();
        expect(mockFn).toHaveBeenCalled();
    });
    it("should return null", () => {
        const error = new Error("Test error");
        const mockFn = jest.fn((_a) => {
            throw error;
        });
        const diagMock = jest.spyOn(api_1.diag, "error");
        const safeFunction = (0, src_1.withSafety)({ fn: mockFn });
        const result = safeFunction(1);
        expect(result).toBeNull();
        expect(mockFn).toHaveBeenCalledWith(1);
        expect(diagMock).toHaveBeenCalledTimes(0);
    });
    it("should log a message and the error when one is passed in", () => {
        const error = new Error("Test error");
        const mockFn = jest.fn((_a) => {
            throw error;
        });
        const diagMock = jest.spyOn(api_1.diag, "error");
        const safeFunction = (0, src_1.withSafety)({
            fn: mockFn,
            onError: (error) => api_1.diag.error(`Test message ${error}`),
        });
        const result = safeFunction(1);
        expect(result).toBeNull();
        expect(mockFn).toHaveBeenCalledWith(1);
        expect(diagMock).toHaveBeenCalledWith("Test message Error: Test error");
    });
});
//# sourceMappingURL=utils.test.js.map