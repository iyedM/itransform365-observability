"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const openinference_semantic_conventions_1 = require("@arizeai/openinference-semantic-conventions");
const src_1 = require("../../src");
const OISpan_1 = require("../../src/trace/trace-config/OISpan");
const api_1 = require("@opentelemetry/api");
const context_async_hooks_1 = require("@opentelemetry/context-async-hooks");
const sdk_trace_node_1 = require("@opentelemetry/sdk-trace-node");
const sdk_trace_base_1 = require("@opentelemetry/sdk-trace-base");
const tracerProvider = new sdk_trace_node_1.NodeTracerProvider();
tracerProvider.register();
describe("OITracer", () => {
    let mockTracer;
    let mockSpan;
    let contextManager;
    const memoryExporter = new sdk_trace_base_1.InMemorySpanExporter();
    tracerProvider.addSpanProcessor(new sdk_trace_base_1.SimpleSpanProcessor(memoryExporter));
    beforeEach(() => {
        contextManager = new context_async_hooks_1.AsyncHooksContextManager().enable();
        api_1.context.setGlobalContextManager(contextManager);
        mockSpan = {
            setAttribute: jest.fn(),
            setAttributes: jest.fn(),
        };
        mockTracer = {
            startSpan: jest.fn(() => mockSpan),
            startActiveSpan: jest.fn((name, options, context, fn) => {
                return fn(mockSpan);
            }),
        };
    });
    beforeEach(() => {
        memoryExporter.reset();
    });
    afterEach(() => {
        api_1.context.disable();
    });
    describe("startSpan", () => {
        it("should create an OISpan with start span and set attributes according to the trace config", () => {
            const oiTracer = new src_1.OITracer({
                tracer: mockTracer,
                traceConfig: {
                    hideInputs: true,
                },
            });
            const name = "test-span";
            const options = {
                attributes: { key1: "value1", "input.value": "sensitiveValue" },
            };
            const span = oiTracer.startSpan(name, options);
            expect(mockTracer.startSpan).toHaveBeenCalledWith(name, { attributes: undefined }, api_1.context.active());
            expect(mockSpan.setAttributes).toHaveBeenCalledWith({
                key1: "value1",
                "input.value": src_1.REDACTED_VALUE,
            });
            expect(span).toBeInstanceOf(OISpan_1.OISpan);
        });
        it("should add OpenInference context attributes to spans", () => {
            const oiTracer = new src_1.OITracer({
                tracer: mockTracer,
                traceConfig: {
                    hideInputs: true,
                },
            });
            const name = "test-span";
            const options = {
                attributes: { key1: "value1", "input.value": "sensitiveValue" },
            };
            api_1.context.with((0, src_1.setSession)(api_1.context.active(), { sessionId: "my-session-id" }), () => {
                const span = oiTracer.startSpan(name, options, api_1.context.active());
                expect(mockTracer.startSpan).toHaveBeenCalledWith(name, { attributes: undefined }, api_1.context.active());
                expect(mockSpan.setAttributes).toHaveBeenCalledWith({
                    key1: "value1",
                    [openinference_semantic_conventions_1.SESSION_ID]: "my-session-id",
                    "input.value": src_1.REDACTED_VALUE,
                });
                expect(span).toBeInstanceOf(OISpan_1.OISpan);
            });
        });
    });
    describe("startActiveSpan", () => {
        it("should create an OISpan with startActiveSpan and set attributes according to the trace config", () => {
            const oiTracer = new src_1.OITracer({
                tracer: mockTracer,
                traceConfig: {
                    hideInputs: true,
                },
            });
            const name = "test-span";
            const options = {
                attributes: { key1: "value1", "input.value": "sensitiveValue" },
            };
            const span = oiTracer.startActiveSpan(name, options, (span) => {
                return span;
            });
            expect(mockTracer.startActiveSpan).toHaveBeenCalledWith(name, {
                attributes: undefined,
            }, expect.any(Object), expect.any(Function));
            expect(mockSpan.setAttributes).toHaveBeenCalledWith({
                key1: "value1",
                "input.value": src_1.REDACTED_VALUE,
            });
            expect(span).toBeInstanceOf(OISpan_1.OISpan);
        });
        it("should handle overloads correctly", () => {
            const oiTracer = new src_1.OITracer({
                tracer: mockTracer,
                traceConfig: {
                    hideInputs: true,
                },
            });
            const name = "test-span";
            const mockFn = jest.fn();
            oiTracer.startActiveSpan(name, mockFn);
            expect(mockTracer.startActiveSpan).toHaveBeenCalledWith(name, { attributes: undefined }, api_1.context.active(), expect.any(Function));
            const options = {
                kind: api_1.SpanKind.INTERNAL,
                attributes: { key: "value" },
            };
            oiTracer.startActiveSpan(name, options, mockFn);
            expect(mockTracer.startActiveSpan).toHaveBeenCalledWith(name, { kind: api_1.SpanKind.INTERNAL, attributes: undefined }, api_1.context.active(), expect.any(Function));
            const newContext = api_1.context.active().setValue(Symbol("test"), "test");
            oiTracer.startActiveSpan(name, options, newContext, mockFn);
            expect(mockTracer.startActiveSpan).toHaveBeenCalledWith(name, { kind: api_1.SpanKind.INTERNAL, attributes: undefined }, newContext, expect.any(Function));
        });
        it("should add OpenInference context attributes to spans", () => {
            const oiTracer = new src_1.OITracer({
                tracer: mockTracer,
                traceConfig: {
                    hideInputs: true,
                },
            });
            const name = "test-span";
            const options = {
                attributes: { key1: "value1", "input.value": "sensitiveValue" },
            };
            api_1.context.with((0, src_1.setSession)(api_1.context.active(), { sessionId: "my-session-id" }), () => {
                const span = oiTracer.startActiveSpan(name, options, (span) => span);
                expect(mockTracer.startActiveSpan).toHaveBeenCalledWith(name, {
                    attributes: undefined,
                }, api_1.context.active(), expect.any(Function));
                expect(mockSpan.setAttributes).toHaveBeenCalledWith({
                    key1: "value1",
                    [openinference_semantic_conventions_1.SESSION_ID]: "my-session-id",
                    "input.value": src_1.REDACTED_VALUE,
                });
                expect(span).toBeInstanceOf(OISpan_1.OISpan);
            });
        });
        it("should properly nest spans", () => {
            const tracer = tracerProvider.getTracer("test");
            const oiTracer = new src_1.OITracer({
                tracer,
                traceConfig: {
                    hideInputs: true,
                },
            });
            oiTracer.startActiveSpan("parent", (parentSpan) => {
                const childSpan = oiTracer.startSpan("child");
                childSpan.end();
                parentSpan.end();
            });
            oiTracer.startSpan("parent2").end();
            const spans = memoryExporter.getFinishedSpans();
            const parentSpan = spans.find((span) => span.name === "parent");
            const childSpan = spans.find((span) => span.name === "child");
            const parent2 = spans.find((span) => span.name === "parent2");
            expect(parentSpan).toBeDefined();
            expect(childSpan).toBeDefined();
            const parentSpanId = parentSpan === null || parentSpan === void 0 ? void 0 : parentSpan.spanContext().spanId;
            expect(parentSpanId).toBeDefined();
            const childSpanParentId = childSpan === null || childSpan === void 0 ? void 0 : childSpan.parentSpanId;
            expect(childSpanParentId).toBeDefined();
            expect(childSpanParentId).toBe(parentSpanId);
            expect(childSpan === null || childSpan === void 0 ? void 0 : childSpan.spanContext().traceId).toBe(parentSpan === null || parentSpan === void 0 ? void 0 : parentSpan.spanContext().traceId);
            expect(parent2 === null || parent2 === void 0 ? void 0 : parent2.spanContext().traceId).not.toBe(childSpan === null || childSpan === void 0 ? void 0 : childSpan.spanContext().traceId);
        });
    });
});
//# sourceMappingURL=OITracer.test.js.map