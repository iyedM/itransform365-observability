"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const constants_1 = require("../../src/trace/trace-config/constants");
const traceConfig_1 = require("../../src/trace/trace-config/traceConfig");
describe("generateTraceConfig", () => {
    it("should return the default trace config when no options are provided", () => {
        expect((0, traceConfig_1.generateTraceConfig)()).toEqual(constants_1.DefaultTraceConfig);
    });
    it("should respect the options provided", () => {
        const options = {
            hideInputText: true,
            hideOutputText: false,
        };
        const expected = Object.assign(Object.assign({}, constants_1.DefaultTraceConfig), { hideInputText: true, hideOutputText: false });
        expect((0, traceConfig_1.generateTraceConfig)(options)).toEqual(expected);
    });
    it("should fallback to environment variables if options are not provided", () => {
        process.env[constants_1.OPENINFERENCE_HIDE_INPUT_TEXT] = "true";
        const expected = Object.assign(Object.assign({}, constants_1.DefaultTraceConfig), { hideInputText: true, hideInputImages: true });
        expect((0, traceConfig_1.generateTraceConfig)({ hideInputImages: true })).toEqual(expected);
        delete process.env[constants_1.OPENINFERENCE_HIDE_INPUT_TEXT];
    });
});
//# sourceMappingURL=traceConfig.test.js.map