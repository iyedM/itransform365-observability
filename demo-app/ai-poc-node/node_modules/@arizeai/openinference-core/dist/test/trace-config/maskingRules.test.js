"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const constants_1 = require("../../src/trace/trace-config/constants");
const maskingRules_1 = require("../../src/trace/trace-config/maskingRules");
const utils_1 = require("../../src/utils");
const generateMaskTestCases = () => {
    const testCases = [];
    Object.keys(constants_1.DefaultTraceConfig).forEach((key) => {
        const configKey = key;
        const ioValue = configKey.includes("Input") ? "input" : "output";
        switch (configKey) {
            case "hideInputMessages":
            case "hideOutputMessages":
            case "hideInputs":
            case "hideOutputs": {
                testCases.push([
                    `should return undefined for "llm.${ioValue}_messages" when ${configKey} is set to true`,
                    Object.assign(Object.assign({}, constants_1.DefaultTraceConfig), { [configKey]: true }),
                    undefined,
                    `llm.${ioValue}_messages.0.message.content`,
                    "some message content",
                ]);
                if (configKey === "hideInputs" || configKey === "hideOutputs") {
                    testCases.push([
                        `should return ${constants_1.REDACTED_VALUE} for ${ioValue}.value when ${configKey} is set to true`,
                        Object.assign(Object.assign({}, constants_1.DefaultTraceConfig), { [configKey]: true }),
                        constants_1.REDACTED_VALUE,
                        `${ioValue}.value`,
                        "some content",
                    ], [
                        `should return undefined for ${ioValue}.mime_type when ${configKey} is set to true`,
                        Object.assign(Object.assign({}, constants_1.DefaultTraceConfig), { [configKey]: true }),
                        undefined,
                        `${ioValue}.mime_type`,
                        "some content",
                    ]);
                }
                break;
            }
            case "hideOutputText":
            case "hideInputText":
                testCases.push([
                    `should return ${constants_1.REDACTED_VALUE} for "llm.${ioValue}_messages.0.message.content" when ${configKey} is set to true`,
                    Object.assign(Object.assign({}, constants_1.DefaultTraceConfig), { [configKey]: true }),
                    constants_1.REDACTED_VALUE,
                    `llm.${ioValue}_messages.0.message.content`,
                    "some message content",
                ], [
                    `should return ${constants_1.REDACTED_VALUE} for "llm.${ioValue}_messages.0.message.contents.0.message_content.text" when ${configKey} is set to true`,
                    Object.assign(Object.assign({}, constants_1.DefaultTraceConfig), { [configKey]: true }),
                    constants_1.REDACTED_VALUE,
                    `llm.${ioValue}_messages.0.message.contents.0.message_content.text`,
                    "some message content",
                ], [
                    `should not change the value for "llm.${ioValue}_messages.0.message.contents.0.message_content.image" when ${configKey} is set to true`,
                    Object.assign(Object.assign({}, constants_1.DefaultTraceConfig), { [configKey]: true }),
                    "some image",
                    `llm.${ioValue}_messages.0.message.contents.0.message_content.image`,
                    "some image",
                ]);
                break;
            case "hideInputImages":
                testCases.push([
                    `should return undefined for "llm.input_messages.0.message.contents.0.message_content.image" when hideInputImages is set to true`,
                    Object.assign(Object.assign({}, constants_1.DefaultTraceConfig), { hideInputImages: true }),
                    undefined,
                    "llm.input_messages.0.message.contents.0.message_content.image",
                    "some image",
                ]);
                break;
            case "hideEmbeddingVectors":
                testCases.push([
                    `should return undefined for "embedding.embeddings.0.embedding.vector" when hideEmbeddingVectors is set to true`,
                    Object.assign(Object.assign({}, constants_1.DefaultTraceConfig), { hideEmbeddingVectors: true }),
                    undefined,
                    "embedding.embeddings.0.embedding.vector",
                    "some embedding vector",
                ]);
                break;
            case "base64ImageMaxLength":
                testCases.push([
                    `should return ${constants_1.REDACTED_VALUE} for "llm.input_messages.0.message.contents.0.message_content.image.url" when the base64 image is too long`,
                    Object.assign(Object.assign({}, constants_1.DefaultTraceConfig), { base64ImageMaxLength: 10 }),
                    constants_1.REDACTED_VALUE,
                    "llm.input_messages.0.message.contents.0.message_content.image.url",
                    "data:image/base64,verylongbase64string",
                ]);
                break;
            default:
                (0, utils_1.assertUnreachable)(configKey);
        }
    });
    return testCases;
};
describe("mask", () => {
    test.each(generateMaskTestCases())("%s", (_, config, expected, key, initialValue) => {
        expect((0, maskingRules_1.mask)({ config, key, value: initialValue })).toEqual(expected);
    });
});
//# sourceMappingURL=maskingRules.test.js.map