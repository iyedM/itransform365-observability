var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { context as apiContext, } from "@opentelemetry/api";
import { OISpan } from "./OISpan";
import { generateTraceConfig } from "./traceConfig";
import { getAttributesFromContext } from "../contextAttributes";
/**
 * Formats the params for the startActiveSpan method
 * The method has multiple overloads, so we need to format the arguments
 * Taken from @see https://github.com/open-telemetry/opentelemetry-js/blob/main/packages/opentelemetry-sdk-trace-base/src/Tracer.ts#L220C3-L235C6
 *
 */
function formatStartActiveSpanParams(arg2, arg3, arg4) {
    var opts;
    var ctx;
    var fn;
    if (typeof arg2 === "function") {
        fn = arg2;
    }
    else if (typeof arg3 === "function") {
        opts = arg2;
        fn = arg3;
    }
    else {
        opts = arg2;
        ctx = arg3;
        fn = arg4;
    }
    opts = opts !== null && opts !== void 0 ? opts : {};
    ctx = ctx !== null && ctx !== void 0 ? ctx : apiContext.active();
    return { opts: opts, ctx: ctx, fn: fn };
}
/**
 * A wrapper around the OpenTelemetry {@link Tracer} interface that masks sensitive information based on the passed in {@link TraceConfig}.
 */
var OITracer = /** @class */ (function () {
    /**
     *
     * @param tracer The OpenTelemetry {@link Tracer} to wrap
     * @param traceConfig The {@link TraceConfigOptions} to set to control the behavior of the tracer
     */
    function OITracer(_a) {
        var tracer = _a.tracer, traceConfig = _a.traceConfig;
        this.tracer = tracer;
        this.config = generateTraceConfig(traceConfig);
    }
    OITracer.prototype.startActiveSpan = function (name, arg2, arg3, arg4) {
        var _this = this;
        var formattedArgs = formatStartActiveSpanParams(arg2, arg3, arg4);
        if (formattedArgs == null) {
            return;
        }
        var opts = formattedArgs.opts, ctx = formattedArgs.ctx, fn = formattedArgs.fn;
        var attributes = (opts !== null && opts !== void 0 ? opts : {}).attributes;
        var contextAttributes = getAttributesFromContext(ctx);
        var mergedAttributes = __assign(__assign({}, contextAttributes), attributes);
        return this.tracer.startActiveSpan(name, __assign(__assign({}, opts), { attributes: undefined }), ctx, function (span) {
            var openInferenceSpan = new OISpan({
                span: span,
                config: _this.config,
            });
            openInferenceSpan.setAttributes(mergedAttributes);
            return fn(openInferenceSpan);
        });
    };
    OITracer.prototype.startSpan = function (name, options, context) {
        var attributes = options === null || options === void 0 ? void 0 : options.attributes;
        var ctx = context !== null && context !== void 0 ? context : apiContext.active();
        var contextAttributes = getAttributesFromContext(ctx);
        var mergedAttributes = __assign(__assign({}, contextAttributes), attributes);
        var span = new OISpan({
            span: this.tracer.startSpan(name, __assign(__assign({}, options), { attributes: undefined }), ctx),
            config: this.config,
        });
        span.setAttributes(mergedAttributes);
        return span;
    };
    return OITracer;
}());
export { OITracer };
//# sourceMappingURL=OITracer.js.map