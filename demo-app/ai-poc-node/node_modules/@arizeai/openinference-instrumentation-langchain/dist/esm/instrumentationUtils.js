import { LangChainTracer } from "./tracer";
export function addTracerToHandlers(tracer, handlers) {
    if (handlers == null) {
        return [new LangChainTracer(tracer)];
    }
    if (Array.isArray(handlers)) {
        var tracerAlreadyRegistered_1 = handlers.some(function (handler) { return handler instanceof LangChainTracer; });
        if (!tracerAlreadyRegistered_1) {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            handlers.push(new LangChainTracer(tracer));
        }
        return handlers;
    }
    var tracerAlreadyRegistered = handlers.inheritableHandlers.some(function (handler) { return handler instanceof LangChainTracer; }) ||
        handlers.handlers.some(function (handler) { return handler instanceof LangChainTracer; });
    if (tracerAlreadyRegistered) {
        return handlers;
    }
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    handlers.addHandler(new LangChainTracer(tracer), true);
    return handlers;
}
//# sourceMappingURL=instrumentationUtils.js.map